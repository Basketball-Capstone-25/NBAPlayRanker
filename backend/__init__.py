"""
Backend package marker.

This file makes the `backend` folder a real Python package so relative imports
like `from .nlp_reasoning import ...` resolve correctly in editors, type
checkers, and when running the app as a package.
"""